For Pancentric use, we add django-taggit-autosuggest so that in the admin
we autocomplete tag names.

The CSS for django-taggit-autosuggest doesn't quite work with djangocms-admin-style,
so I've copied the whole CSS file over from django-taggit-autosuggest and
customised it.